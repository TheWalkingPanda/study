server:
  port: 8888
spring:
  application:
    name: Spring boot Admin Web
  boot:
    admin:
      url: http://localhost:${server.port}
  jackson:
    serialization:
      indent_output: true
endpoints:
  health:
    sensitive: false
management:
  security:
    enabled: false